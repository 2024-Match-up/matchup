import 'package:flutter/material.dart';

class LegStretchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('다리 스트레칭'),
      ),
      body: Center(
        child: Text('Here is some detailed information about 다리 스트레칭.'),
      ),
    );
  }
}
