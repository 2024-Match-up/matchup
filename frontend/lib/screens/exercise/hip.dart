import 'package:flutter/material.dart';

class HipStretchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('골반 스트레칭'),
      ),
      body: Center(
        child: Text('Here is some detailed information about 골반 스트레칭.'),
      ),
    );
  }
}
