import 'package:flutter/material.dart';


class GradeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Grade'),
      ),
      body: Center(
        child: Text('Grade Screen'),
      ),
    );
  }
}